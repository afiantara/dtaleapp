version: '3.8'
services:
  dtaleapp:
    container_name: dtaleapp
    restart: always
    env_file: .env
    build: .
    networks:
      - db_network
      - web_network
  nginx:
    container_name: nginx
    restart: always
    image: "nginx:latest"
    ports:
      - "5080:5080"
    volumes:
      - ./nginx:/etc/nginx/conf.d
    networks:
      - web_network
    depends_on: 
      - dtaleapp
networks:
  db_network:
    driver: bridge
  web_network:
    driver: bridge
 